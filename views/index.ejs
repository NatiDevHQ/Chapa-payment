<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Payment Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      rel="stylesheet"
    />
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              primary: "#000000",
              secondary: "#ffffff",
              accent: "#404040",
              light: "#f8f8f8",
              dark: "#1a1a1a",
            },
          },
        },
      };
    </script>
  </head>
  <body
    class="bg-gradient-to-br from-gray-100 to-gray-200 min-h-screen flex items-center justify-center p-6"
  >
    <div class="max-w-md w-full">
      <!-- Status Card -->
      <div
        class="bg-white rounded-2xl shadow-2xl p-8 transform transition-all duration-300 hover:scale-105 border border-gray-200"
      >
        <!-- Status Indicator -->
        <div
          class="w-24 h-24 rounded-full mx-auto mb-6 flex items-center justify-center text-4xl text-white <%= paymentStatus === 'pending' ? 'bg-gray-600' : '' %> <%= paymentStatus === 'complete' ? 'bg-black' : '' %> <%= !['pending', 'complete'].includes(paymentStatus) ? 'bg-gray-800' : '' %> border-4 border-white shadow-lg"
        >
          <% if (paymentStatus === 'pending') { %>
          <i class="fas fa-clock"></i>
          <% } else if (paymentStatus === 'complete') { %>
          <i class="fas fa-check"></i>
          <% } else { %>
          <i class="fas fa-exclamation"></i>
          <% } %>
        </div>

        <!-- Status Title -->
        <h1 class="text-2xl font-bold text-center text-gray-900 mb-3">
          <% if (paymentStatus === 'pending') { %> Payment Pending <% } else if
          (paymentStatus === 'complete') { %> Payment Complete! <% } else { %>
          Action Required <% } %>
        </h1>

        <!-- Status Description -->
        <p class="text-gray-600 text-center mb-8 leading-relaxed">
          <% if (paymentStatus === 'pending') { %> Your subscription payment is
          pending. Complete the payment process to continue enjoying our
          services. <% } else if (paymentStatus === 'complete') { %> Thank you!
          Your payment has been successfully processed. You're all set! <% }
          else { %> Your payment deadline is approaching. Please complete your
          payment to avoid service interruption. <% } %>
        </p>

        <!-- Payment Info -->
        <div class="bg-gray-50 rounded-xl p-6 mb-6 border border-gray-200">
          <div
            class="flex justify-between items-center py-3 border-b border-gray-300"
          >
            <span class="font-semibold text-gray-800">Current Status:</span>
            <span
              class="<%= paymentStatus === 'pending' ? 'text-gray-600' : '' %> <%= paymentStatus === 'complete' ? 'text-black font-bold' : '' %> <%= !['pending', 'complete'].includes(paymentStatus) ? 'text-gray-800' : '' %> font-medium"
            >
              <% if (paymentStatus === 'pending') { %> Pending <% } else if
              (paymentStatus === 'complete') { %> Active <% } else { %> Overdue
              <% } %>
            </span>
          </div>
          <div
            class="flex justify-between items-center py-3 border-b border-gray-300"
          >
            <span class="font-semibold text-gray-800">Next Billing:</span>
            <span class="text-gray-600">30 days</span>
          </div>
          <div class="flex justify-between items-center py-3">
            <span class="font-semibold text-gray-800">Last Updated:</span>
            <span class="text-gray-600"
              ><%= new Date().toLocaleDateString() %></span
            >
          </div>
        </div>

        <!-- Action Button -->
        <a
          href="/plans"
          class="block w-full bg-black hover:bg-gray-800 text-white font-bold py-4 px-6 rounded-xl transition-all duration-300 transform hover:scale-105 hover:shadow-xl text-center border border-gray-800"
        >
          <% if (paymentStatus === 'complete') { %>
          <i class="fas fa-cog mr-2"></i>Manage Subscription <% } else { %>
          <i class="fas fa-arrow-right mr-2"></i>Continue to Payment <% } %>
        </a>
      </div>
    </div>
  </body>
</html>
